#!/usr/bin/env python
"""Splits up the .dot file generated by the module.

After it has split up these files, run:
	for f in `ls output.*.dot`; do dot -Tpng -o$f.png $f; done
to convert the files to PNG images.
"""

fin = open('output.dot')

fout = open('/dev/null', 'w')
part = 1
for line in fin:
	if line[0] == '#':
		fout.close()
		fout = open('output.{0}.dot'.format(part), 'w')
		part += 1
	fout.write(line)
fout.close()
